//#version 330

//layout(location=0) in vec3 position;
//layout(location=1) in vec3 normal;
//layout(location=3) in vec3 color;

// Uniforms
// uniform mat4 Model;
//uniform mat4 View;
//uniform mat4 Projection;
//uniform float Time;

// Outputs to fragment shader
//out vec3 f_color;

//void main()
// {
    // Create a time-varying offset using a sine function
    //float offset = sin(Time) * 0.5;

    // Adjust the position based on time (e.g., move up and down)
    //vec3 animatedPosition = position + vec3(0.0, offset, 0.0);

    // Compute gl_Position
    //gl_Position = Projection * View * Model * vec4(animatedPosition, 1.0);

    // Vary the color based on time
  //  f_color = color * abs(sin(Time));
//}
